rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEV: permissive (authenticated users can read/write)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

    // PROD (skeleton): restrict to chat membership
    // match /chats/{chatId}/messages/{messageId} {
    //   allow read, create: if request.auth != null && isMemberOfChat(chatId);
    //   allow update, delete: if false; // no edits/deletes for now
    // }
    // function isMemberOfChat(chatId) {
    //   return exists(/databases/$(database)/documents/chats/$(chatId)/members/$(request.auth.uid));
    // }
  }
}
